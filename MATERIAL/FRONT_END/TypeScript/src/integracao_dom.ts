const captureNome = document.getElementById("nome");
const form = document.getElementsByTagName("form");
const btnSend = document.getElementById("btnSend");

btnSend?.addEventListener("click", (e: MouseEvent) => {
  e.pageY;
});
